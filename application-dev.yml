spring:
  application:
    name: config-dev
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true      # ✅ FIXED: correct spelling
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka  # ✅ Perfect

